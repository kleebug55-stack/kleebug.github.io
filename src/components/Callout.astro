---
interface Props {
  type?: "insight" | "note" | "warning";
}

const { type = "note" } = Astro.props;
const labels = {
  insight: "关键洞察",
  note: "花园旁注",
  warning: "需要留意",
};
---

<aside class:list={["callout", type]}>
  <p class="label">{labels[type]}</p>
  <div>
    <slot />
  </div>
</aside>

<style>
  .callout {
    margin: var(--space-8) 0;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background:
      linear-gradient(135deg, rgb(255 252 245 / 0.92), rgb(243 236 223 / 0.72)),
      var(--surface-background);
    padding: var(--space-5);
    box-shadow: 0 12px 28px rgb(73 54 28 / 0.07);
  }

  .insight {
    border-color: rgb(var(--color-accent-rgb) / 0.25);
    background:
      linear-gradient(135deg, rgb(var(--color-accent-rgb) / 0.12), transparent 62%),
      rgb(255 252 245 / 0.86);
  }

  .warning {
    border-color: rgb(220 38 38 / 0.22);
    background:
      linear-gradient(135deg, rgb(220 38 38 / 0.08), transparent 62%),
      rgb(255 252 245 / 0.86);
  }

  .label {
    margin: 0 0 0.4rem;
    color: var(--color-primary);
    font-size: 0.85rem;
    font-weight: 900;
  }

  .callout :global(p:last-child) {
    margin-bottom: 0;
  }
</style>
